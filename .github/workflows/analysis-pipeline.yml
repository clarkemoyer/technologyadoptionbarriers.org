name: TABS V2 Analysis Pipeline

# All-Python research pipeline: export → enrich → triage → de-identify → analyze.
# Runs daily before the disposition pipeline (which handles Prolific operations).
#
# Data flow:
#   Qualtrics API → raw CSV
#   Prolific API  → auth checks + submission statuses
#   Enrich raw CSV with Prolific data
#   Disposition triage (Python port, cross-validates with TS pipeline)
#   De-identify → public CSV (NIST 5-step, for ScholarSphere)
#   Run full analysis suite on enriched CSV:
#     - Data audit (validate waterfall)
#     - Descriptive + sensitivity analysis (all 5 sample definitions)
#     - Advanced/inferential (PCA, regression, ANOVA)
#     - Psychometrics (KMO, HTMT, Cronbach's, Harman's)
#     - Quality audit (outliers, CMV, Mahalanobis)
#   Output JSON for website → PR

on:
  schedule:
    - cron: '0 9 * * *' # Daily at 09:00 UTC (4am ET / 5am ET DST)
  workflow_dispatch:

concurrency:
  group: analysis-pipeline
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  # ── Phase 1a: Export Qualtrics data ───────────────────────────
  export-qualtrics:
    name: Export Qualtrics
    runs-on: ubuntu-latest
    environment: qualtrics-prod
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Export Qualtrics CSV
        run: |
          python -c "
          from scripts.analysis.tabs_api import qualtrics_export_csv
          import os
          qualtrics_export_csv(
              os.environ['QUALTRICS_API_TOKEN'],
              os.environ['QUALTRICS_BASE_URL'],
              os.environ['QUALTRICS_SURVEY_ID'],
              '${{ runner.temp }}/qualtrics-raw.csv',
          )
          "
        env:
          QUALTRICS_API_TOKEN: ${{ secrets.QUALTRICS_API_TOKEN }}
          QUALTRICS_BASE_URL: ${{ vars.QUALTRICS_BASE_URL }}
          QUALTRICS_SURVEY_ID: ${{ vars.QUALTRICS_SURVEY_ID }}
          PYTHONPATH: ${{ github.workspace }}

      - uses: actions/upload-artifact@v7
        with:
          name: qualtrics-raw
          path: ${{ runner.temp }}/qualtrics-raw.csv
          retention-days: 1

  # ── Phase 1b: Fetch Prolific data ────────────────────────────
  fetch-prolific:
    name: Fetch Prolific Data
    runs-on: ubuntu-latest
    environment: prolific-prod
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Fetch auth checks
        run: |
          python -c "
          from scripts.analysis.tabs_api import prolific_auth_checks_csv
          import os
          prolific_auth_checks_csv(
              os.environ['PROLIFIC_STUDY_ID'],
              os.environ['PROLIFIC_API_TOKEN'],
              '${{ runner.temp }}/auth-checks.csv',
          )
          "
        env:
          PROLIFIC_API_TOKEN: ${{ secrets.TABS_PROLIFIC_TOKEN }}
          PROLIFIC_STUDY_ID: ${{ vars.PROLIFIC_STUDY_ID }}
          PYTHONPATH: ${{ github.workspace }}

      - name: Fetch submission statuses
        run: |
          python -c "
          from scripts.analysis.tabs_api import prolific_submission_statuses
          import os, json
          statuses = prolific_submission_statuses(
              os.environ['PROLIFIC_STUDY_ID'],
              os.environ['PROLIFIC_API_TOKEN'],
          )
          with open('${{ runner.temp }}/statuses.json', 'w') as f:
              json.dump(statuses, f)
          print(f'  Statuses: {len(statuses)} submissions')
          "
        env:
          PROLIFIC_API_TOKEN: ${{ secrets.TABS_PROLIFIC_TOKEN }}
          PROLIFIC_STUDY_ID: ${{ vars.PROLIFIC_STUDY_ID }}
          PYTHONPATH: ${{ github.workspace }}

      # Fetch demographics (best-effort — some studies may not have data)
      - name: Fetch demographics
        continue-on-error: true
        run: |
          python -c "
          from scripts.analysis.tabs_api import prolific_demographics_csv
          import os
          result = prolific_demographics_csv(
              os.environ['PROLIFIC_STUDY_ID'],
              os.environ['PROLIFIC_API_TOKEN'],
              '${{ runner.temp }}/demographics.csv',
          )
          if not result:
              print('  Demographics not available, skipping')
          "
        env:
          PROLIFIC_API_TOKEN: ${{ secrets.TABS_PROLIFIC_TOKEN }}
          PROLIFIC_STUDY_ID: ${{ vars.PROLIFIC_STUDY_ID }}
          PYTHONPATH: ${{ github.workspace }}

      - uses: actions/upload-artifact@v7
        with:
          name: prolific-data
          path: |
            ${{ runner.temp }}/auth-checks.csv
            ${{ runner.temp }}/statuses.json
            ${{ runner.temp }}/demographics.csv
          retention-days: 1
          if-no-files-found: warn

  # ── Phase 2: Enrich + Analyze ────────────────────────────────
  analyze:
    name: Enrich & Analyze
    needs: [export-qualtrics, fetch-prolific]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: pip install -r scripts/analysis/requirements.txt

      - uses: actions/download-artifact@v8
        with:
          name: qualtrics-raw
          path: ${{ runner.temp }}

      - uses: actions/download-artifact@v8
        with:
          name: prolific-data
          path: ${{ runner.temp }}

      # Enrich Qualtrics CSV with Prolific data (auth checks + statuses + demographics)
      - name: Enrich CSV with Prolific data
        run: |
          DEMO_FLAG=""
          if [ -f "${{ runner.temp }}/demographics.csv" ]; then
            DEMO_FLAG="--demographics ${{ runner.temp }}/demographics.csv"
          fi
          python scripts/analysis/enrich_qualtrics_csv.py \
            --qualtrics "${{ runner.temp }}/qualtrics-raw.csv" \
            --auth-checks "${{ runner.temp }}/auth-checks.csv" \
            --statuses "${{ runner.temp }}/statuses.json" \
            $DEMO_FLAG \
            --output "${{ runner.temp }}/qualtrics-enriched.csv"

      # Data audit — validate Python disposition matches TS pipeline
      - name: Data audit (disposition waterfall)
        run: |
          python scripts/analysis/tabs_v2_data_audit.py \
            --input "${{ runner.temp }}/qualtrics-enriched.csv" \
            --output "${{ runner.temp }}/data-audit.json"

      # De-identify for public release
      - name: De-identify dataset
        run: |
          python scripts/deidentify_tabs_data.py \
            "${{ runner.temp }}/qualtrics-enriched.csv" \
            "${{ runner.temp }}/deidentified" \
            --skip-review

      # Sensitivity analysis (all 5 sample definitions)
      - name: Descriptive analysis + sensitivity
        run: |
          python scripts/analysis/tabs_v2_analysis.py \
            "${{ runner.temp }}/qualtrics-enriched.csv" \
            --json "${{ runner.temp }}/sensitivity-analysis.json"

      # Advanced/inferential analysis
      - name: Advanced analysis (PCA, regression, ANOVA)
        continue-on-error: true
        run: |
          python scripts/analysis/tabs_v2_advanced.py \
            "${{ runner.temp }}/qualtrics-enriched.csv" \
            > "${{ runner.temp }}/advanced-analysis.txt" 2>&1
          echo "Advanced analysis complete"

      # Psychometric validation
      - name: Psychometrics (KMO, HTMT, reliability)
        continue-on-error: true
        run: |
          python scripts/analysis/tabs_v2_psychometrics.py \
            "${{ runner.temp }}/qualtrics-enriched.csv" \
            > "${{ runner.temp }}/psychometrics.txt" 2>&1
          echo "Psychometrics complete"

      # Quality audit
      - name: Quality audit (outliers, CMV)
        continue-on-error: true
        run: |
          python scripts/analysis/tabs_v2_quality_audit.py \
            "${{ runner.temp }}/qualtrics-enriched.csv" \
            > "${{ runner.temp }}/quality-audit.txt" 2>&1
          echo "Quality audit complete"

      # Summary for GitHub Actions UI
      - name: Generate summary
        if: always()
        run: |
          echo "### Analysis Pipeline Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          if [ -f "${{ runner.temp }}/sensitivity-analysis.json" ]; then
            python -c "
          import json
          data = json.load(open('${{ runner.temp }}/sensitivity-analysis.json'))
          print('#### Sample Definitions')
          print('')
          print('| Sample | N |')
          print('| --- | ---: |')
          for s in data['samples']:
              print(f\"| {s['label']} | {s['n']} |\")
          " >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ -f "${{ runner.temp }}/data-audit.json" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "#### Disposition Waterfall" >> "$GITHUB_STEP_SUMMARY"
            python -c "
          import json
          data = json.load(open('${{ runner.temp }}/data-audit.json'))
          print('')
          print('| Disposition | Count |')
          print('| --- | ---: |')
          for d, c in sorted(data.get('disposition_counts', {}).items(), key=lambda x: -x[1]):
              print(f'| {d} | {c} |')
          " >> "$GITHUB_STEP_SUMMARY"
          fi

      # Upload analysis outputs for website
      - uses: actions/upload-artifact@v7
        with:
          name: analysis-results
          path: |
            ${{ runner.temp }}/sensitivity-analysis.json
            ${{ runner.temp }}/data-audit.json
          retention-days: 7

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: analysis-logs
          path: |
            ${{ runner.temp }}/advanced-analysis.txt
            ${{ runner.temp }}/psychometrics.txt
            ${{ runner.temp }}/quality-audit.txt
          retention-days: 7

      # Public CSV for ScholarSphere (30-day retention)
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: public-dataset
          path: ${{ runner.temp }}/deidentified/TABS_V2_public_dataset.csv
          retention-days: 30
          if-no-files-found: warn

  # ── Phase 3: Commit results to repo ───────────────────────────
  commit-results:
    name: Update Website Data
    needs: analyze
    runs-on: ubuntu-latest
    environment: copilot
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v8
        with:
          name: analysis-results
          path: src/data

      - uses: ./.github/actions/format-and-pr
        with:
          token: ${{ secrets.COPILOT_MCP_GITHUB_PERSONAL_ACCESS_TOKEN }}
          files: 'src/data/sensitivity-analysis.json src/data/data-audit.json'
          commit-message: 'chore: update analysis results from daily pipeline'
          pr-title: 'Update analysis results (sensitivity + audit)'
          pr-body: |
            Automated update from the daily analysis pipeline.

            **Data flow**: Qualtrics export → Prolific enrichment (auth checks +
            statuses) → disposition audit → de-identification → full analysis
            suite (descriptive, advanced, psychometrics, quality).

            All statistics computed across 5 sample definitions: Pipeline CLEAN,
            Conservative Clean, Relaxed, All V2 Finished, All V2.
          branch: chore/analysis-results
