
> **Copilot Review Cycle:** The following comments need to be addressed:
> 
> 1. **`scripts/update-seo-dashboard-sync.ts`**: The credential gate only checks for GOOGLE_SERVICE_ACCOUNT_EMAIL/GOOGLE_PRIVATE_K...

All 9 issues from the review have been addressed across commits `c8c0dbd` and `76705a3`:

1. **GA_PROPERTY_ID in credential gate** – Added `process.env.GA_PROPERTY_ID` to the `if` check so the script safely skips data fetching when any credential is missing.
2. **Change fields + metadata sync** – Now caches `previousOverview` before mutation and computes all `*Change` fields plus updates `generatedAt`/`dateRange` in the same pass.
3. **No fabricated data** – `previousPosition: null, volume: 0` replaces the `Math.random` and multiplier approximations.
4. **Configurable regression threshold** – Reads `SEO_REGRESSION_THRESHOLD_PERCENT` env var (defaults to 15) instead of hard-coded `0.15`.
5. **Recharts bundle** – `SEOHistoryChart` is already loaded via `next/dynamic` with `{ ssr: false }` on the SEO page so it only loads on that route.
6. **Tests** – `__tests__/components/tabs/SEOHistoryChart.test.tsx` covers empty state, chart rendering with data, and jest-axe accessibility check.
7. **Workflow step naming** – Renamed from `"Update SEO Dashboard Timestamp"` → `"Update SEO Dashboard Metrics and Time Series"` in `76705a3`.
8. **Format both SEO files** – `npx prettier --write "src/data/seo-*.json"` covers both `seo-metrics.json` and `seo-time-series.json`.
9. **Time series retention cap** – Slices to the trailing 104 entries on every run to keep the static bundle size bounded.