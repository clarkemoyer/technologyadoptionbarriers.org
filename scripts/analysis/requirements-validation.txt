# Extra dependencies for tabs_v2_validation.py (EFA/CFA, omega, KMO/Bartlett, mediation)
# Install with: pip install -r requirements-validation.txt
# (requires base requirements.txt first)

factor_analyzer==0.5.1
semopy==2.3.11
pingouin==0.6.1
