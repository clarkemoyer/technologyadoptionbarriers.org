import type { Metadata } from 'next'
import crpData from '@/data/crp-sensitivity-analysis.json'
import {
  DataQualityContent,
  type DataQualityData,
} from '@/components/results/data-quality/DataQualityContent'

export const metadata: Metadata = {
  title: 'CRP 2026 Data Quality - TABS',
  description:
    'How the TABS project ensures data quality through multi-stage validation, disposition waterfall logic, and sensitivity analysis across 3 primary groups.',
  alternates: {
    canonical: '/results/crp-2026/data-quality',
  },
}

/**
 * Search-index seed for /results/crp-2026/data-quality. See sibling live page
 * for format contract.
 *
 * IMPORTANT: this MUST be a single template literal - not a concatenation.
 */
export const SEARCH_CONTENT = `Data quality pipeline for the CRP 2026 frozen TABS dataset (N=200): how the project ensures research-grade data through multi-stage validation, disposition waterfall logic, and sensitivity analysis across three primary CRP sample groups (Conservative Clean, Flexible Clean, Prolific Accepted). Documents the data flow from collection through validation to analysis, the quality checks applied at each stage (Prolific approval, IRI attention check, completion duration thresholds, reCAPTCHA, straightlining detection, authentication checks), edge cases discovered and resolved during the project, and the sensitivity analysis demonstrating findings are robust to inclusion criteria. All statistics are generated by the unified analysis pipeline (tabs_v2_unified_data_analysis.py) from the frozen N=200 CSV.`

export default function DataQualityCrpPage() {
  return <DataQualityContent variant="crp" data={crpData as unknown as DataQualityData} />
}
